import{_ as t}from"./chunks/sts2-seed-rng-flow.B2Jeyjag.js";import{_ as a,o,c as r,ai as n}from"./chunks/framework.DyvBZb4W.js";const f=JSON.parse('{"title":"Slay the Spire 2 Seed and RNG Mechanics","description":"","frontmatter":{},"headers":[],"relativePath":"en/knowledge/sts2/seed-rng.md","filePath":"en/knowledge/sts2/seed-rng.md","lastUpdated":1778402328000}'),s={name:"en/knowledge/sts2/seed-rng.md"};function d(l,e,i,c,h,m){return o(),r("div",null,[...e[0]||(e[0]=[n('<h1 id="slay-the-spire-2-seed-and-rng-mechanics" tabindex="-1">Slay the Spire 2 Seed and RNG Mechanics <a class="header-anchor" href="#slay-the-spire-2-seed-and-rng-mechanics" aria-label="Permalink to &quot;Slay the Spire 2 Seed and RNG Mechanics&quot;">​</a></h1><p>In Slay the Spire 2, a seed is best understood as the random starting point for a run. It influences map layout, rewards, shops, shuffles, monster behavior, and many other systems, but it is not a complete script for the entire run.</p><p>For players, the useful takeaway is simple: a seed helps reproduce the broad shape of a run, but final outcomes still depend on character, path, choices, deck state, relics, game version, mods, and multiplayer player identity.</p><h2 id="what-players-actually-see" tabindex="-1">What Players Actually See <a class="header-anchor" href="#what-players-actually-see" aria-label="Permalink to &quot;What Players Actually See&quot;">​</a></h2><p>The same seed often helps reproduce:</p><ul><li>The broad map layout, paths, and room distribution.</li><li>Reward tendencies when route and choices are the same.</li><li>Some shop, treasure, event, and reward outcomes.</li><li>Combat shuffles, random generation, and random monster branches.</li></ul><p>But later outcomes may change if you change what happens during the run:</p><ul><li>A different route changes rooms and reward pools.</li><li>A different event choice changes later state and candidates.</li><li>Adding or removing one card can change later shuffles and random card selection.</li><li>Different mods can change card pools, relic pools, events, or logic hooks.</li><li>In multiplayer, some player-specific results may be derived separately per player.</li></ul><p>So “same seed, same run” really means: many random results can be reproduced when version, mods, character, path, choices, and key game state are also close enough.</p><h2 id="why-a-seed-alone-does-not-reveal-everything" tabindex="-1">Why a Seed Alone Does Not Reveal Everything <a class="header-anchor" href="#why-a-seed-alone-does-not-reveal-everything" aria-label="Permalink to &quot;Why a Seed Alone Does Not Reveal Everything&quot;">​</a></h2><p>A seed defines where a random sequence starts. It does not define what the game is choosing from.</p><p>For example, reward cards are not decided by seed alone. The game first builds a candidate pool from character, unlocks, mode, multiplayer constraints, relics, mods, and other filters. Then it randomly chooses from that pool. If the pool changes, the visible result can change even if the random position is the same.</p><p>The same applies to shuffling. The same random order applied to a different deck produces a different visible draw pile. If you removed or added one card earlier, later shuffles may no longer look identical.</p><h2 id="how-to-reproduce-a-run-more-reliably" tabindex="-1">How to Reproduce a Run More Reliably <a class="header-anchor" href="#how-to-reproduce-a-run-more-reliably" aria-label="Permalink to &quot;How to Reproduce a Run More Reliably&quot;">​</a></h2><p>If you want to reproduce a situation, record more than the seed:</p><ul><li>Game version.</li><li>Mod list and mod versions.</li><li>Character, ascension, and game mode.</li><li>Route and major room choices.</li><li>Important reward choices, removes, transforms, and shop purchases.</li><li>Multiplayer player position or player id, if relevant.</li></ul><p>For debugging or mod development, also keep the save file, logs, or RNG state when possible. Most players do not need that extra layer.</p><h2 id="how-god-mod-can-use-this-information" tabindex="-1">How God Mod Can Use This Information <a class="header-anchor" href="#how-god-mod-can-use-this-information" aria-label="Permalink to &quot;How God Mod Can Use This Information&quot;">​</a></h2><p>Player-friendly features could include:</p><table tabindex="0"><thead><tr><th>Feature</th><th>What it helps with</th><th>Risk</th></tr></thead><tbody><tr><td>Seed viewer / copy</td><td>Share the current run, record a test case</td><td>Read-only, low risk</td></tr><tr><td>Current act map seed info</td><td>Help reproduce one act map</td><td>Read-only, low risk</td></tr><tr><td>Reproduction export</td><td>Collect seed, character, mods, route, and key context into a report</td><td>Read-only, useful for bug reports</td></tr><tr><td>Safe preview</td><td>Preview next shuffle, unknown room, reward, or shop tendency</td><td>Must be implemented carefully so it does not change real RNG state</td></tr></tbody></table><p>Advanced features like resetting, fast-forwarding, or replacing RNG streams directly affect future outcomes and may break multiplayer sync. They belong in expert/debug tooling, not the normal player path.</p><h2 id="implementation-details" tabindex="-1">Implementation Details <a class="header-anchor" href="#implementation-details" aria-label="Permalink to &quot;Implementation Details&quot;">​</a></h2><blockquote><p>This section is about code-level implementation. If you only care about what seeds mean for normal play, you can skip it; the sections above are enough.</p></blockquote><p>At the implementation level, STS2 does not drive every system from one global random stream. It derives multiple RNG streams from the same string seed. Each stream has its own seed and counter. Every random number, shuffle, or random selection advances the relevant stream counter.</p><figure class="rng-diagram"><img src="'+t+'" alt="Flow chart showing how a Slay the Spire 2 seed derives multiple RNG streams"><figcaption>A single string seed feeds run-level and player-level RNG streams. Predicting future outcomes requires both the current game state and the relevant stream counter.</figcaption></figure><p>The code-level layers are:</p><table tabindex="0"><thead><tr><th>Layer</th><th>Mechanism</th><th>Typical use</th></tr></thead><tbody><tr><td>String seed</td><td>Stored as <code>StringSeed</code>, then converted into a stable numeric seed</td><td>Sharing, reproduction, derived RNG streams</td></tr><tr><td><code>Rng</code></td><td>Each stream stores <code>Seed</code> and <code>Counter</code>; every random call advances the counter</td><td>Save, load, fast-forward, and reproduce one stream</td></tr><tr><td><code>RunRngSet</code></td><td>Derives run-level streams from the run seed</td><td>Map, shuffle, unknown rooms, combat generation, monster AI, treasure relics</td></tr><tr><td><code>PlayerRngSet</code></td><td>Derives player-level streams from run seed plus player <code>NetId</code></td><td>Rewards, shops, transformations</td></tr><tr><td>Map RNG</td><td>Each act map derives from the run seed with an <code>act_N_map</code> name</td><td>Act map layout, paths, and room type distribution</td></tr></tbody></table><p>Common run-level streams include:</p><ul><li><code>Shuffle</code>: initial combat shuffle, discard-to-draw shuffle, and some random card positions.</li><li><code>UnknownMapPoint</code>: unknown room type rolls and related odds.</li><li><code>CombatCardGeneration</code>: random cards generated during combat.</li><li><code>CombatPotionGeneration</code>: random potions generated during combat.</li><li><code>CombatCardSelection</code>: random selection from existing cards during combat.</li><li><code>CombatEnergyCosts</code>, <code>CombatTargets</code>, <code>CombatOrbs</code>: random combat costs, targets, or orb-related results.</li><li><code>MonsterAi</code>: random monster move branches.</li><li><code>TreasureRoomRelics</code>: shared treasure room relic synchronization.</li><li><code>UpFront</code>: early setup, room pools, relic pools, and ancient-related setup.</li><li><code>Niche</code>: miscellaneous stable random choices.</li></ul><p>Player-level streams are fewer but important:</p><ul><li><code>Rewards</code>: reward cards, reward relic rarity, and some event rewards.</li><li><code>Shops</code>: shop cards, potions, relics, and price variance.</li><li><code>Transformations</code>: card transformations.</li></ul><h2 id="why-future-preview-must-clone-rng" tabindex="-1">Why Future Preview Must Clone RNG <a class="header-anchor" href="#why-future-preview-must-clone-rng" aria-label="Permalink to &quot;Why Future Preview Must Clone RNG&quot;">​</a></h2><p>If a tool wants to preview the future, it should not call the live game RNG directly. It should copy the current stream&#39;s <code>Seed + Counter</code> and simulate the next result on a temporary clone.</p><p>The reason is simple: every real RNG call advances the counter. If preview code consumes the real stream, the act of previewing changes the future it was trying to show.</p><p>A safe preview flow is:</p><ol><li>Read the live RNG seed and counter.</li><li>Create a temporary RNG clone with the same state.</li><li>Simulate reward, shuffle, unknown room, or shop results on the clone.</li><li>Show the preview.</li><li>Discard the clone and leave real game state untouched.</li></ol>',36)])])}const y=a(s,[["render",d]]);export{f as __pageData,y as default};
